name: CI

# Runs on every PR, and on main as a safety net for anything pushed directly.
on:
  pull_request:
  push:
    branches: [main]

# A newer push to the same branch cancels the run already in flight — no point
# spending minutes verifying a commit that has already been superseded.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Typecheck / Lint / Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # `npm ci` not `npm install`: installs exactly what package-lock.json
      # pins, and fails if the lockfile and package.json disagree. CI should
      # build the dependency tree you committed, never resolve a fresh one.
      - run: npm ci

      # Next caches compiled output in .next/cache. Restoring it between runs
      # means only changed modules recompile — the same trick Vercel uses on
      # its own builders.
      - name: Restore Next.js build cache
        uses: actions/cache@v4
        with:
          path: .next/cache
          key: next-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-${{ hashFiles('**/*.ts', '**/*.tsx') }}
          restore-keys: |
            next-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-
            next-${{ runner.os }}-

      - name: Typecheck
        run: npm run typecheck

      # Blocking from the day it was added. The amenity vocabulary fails silently
      # when it's wrong -- a bad key returns no rows rather than throwing -- so a
      # red suite here has to stop the merge.
      - name: Test
        run: npm test

      # The booking map converts between Melbourne wall-clock time and UTC. Code
      # that accidentally reads the machine's own clock passes on a machine whose
      # zone happens to match -- the author's does, in winter. So the whole suite
      # runs again under zones chosen to break that: negative offset, a half-hour
      # offset, the +14 date-line extreme, and Santiago, whose clocks change at
      # midnight so "local midnight" sometimes doesn't exist.
      - name: Test under hostile timezones
        run: |
          for tz in UTC America/New_York Asia/Kolkata Pacific/Kiritimati America/Santiago; do
            echo "::group::TZ=$tz"
            TZ=$tz npx vitest run || exit 1
            echo "::endgroup::"
          done

      # A real gate: the backlog that used to make this report-only is cleared,
      # and lint is clean. A new error now stops the merge like a failing test.
      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          # These only need to *exist* for the build to compile. Every route in
          # this app is dynamic, so nothing queries Supabase or Stripe while
          # building — but lib/stripe.ts constructs its client at module scope
          # and throws on an absent key, which would fail the build here.
          # Real values live in Vercel, which does the actual deploy.
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY: placeholder-anon-key
          SUPABASE_SERVICE_ROLE_KEY: placeholder-service-key
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: pk_test_placeholder
          STRIPE_SECRET_KEY: sk_test_placeholder
          STRIPE_WEBHOOK_SECRET: whsec_placeholder
          RESEND_API_KEY: re_placeholder
          NEXT_PUBLIC_BASE_URL: http://localhost:3000
          CRON_SECRET: placeholder
